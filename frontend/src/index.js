// Biblioteca global para construção de interfaces (parte "lógica", genérica)
import React from 'react';
// Biblioteca do ReactJS para construção de interfaces Web (parte específica)
import ReactDOM from 'react-dom';
import App from './App';

// Renderiza o componente App na tag que contém o ID 'root'
ReactDOM.render(<App />, document.getElementById('root'));
